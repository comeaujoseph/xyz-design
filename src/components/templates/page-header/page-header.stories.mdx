import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';

import PageHeader from './base';

import Button from '../../atoms/button/base';
import ButtonIcon from '../../atoms/button-icon/base';
import IconHelp from '../../../shared/svg-icons/icon-help';

<Meta title="Page Header" component={PageHeader} />

# Page Header

<Preview withToolbar>
  <Story name="base" parameters={{ decorators: [withKnobs] }}>
    <PageHeader name="Alerts" />
  </Story>
</Preview>

<Preview withToolbar>
  <Story name="with actions" parameters={{ decorators: [withKnobs] }}>
    <PageHeader name="Alerts">
        <Button isOutlineTheme>Refresh</Button>
        <Button isTheme>Create New</Button>
        <ButtonIcon size="medium" isNeutral>
            <IconHelp />
        </ButtonIcon>
    </PageHeader>
  </Story>
</Preview>

<Preview withToolbar>
  <Story name="actions" parameters={{ decorators: [withKnobs] }}>
    <PageHeader>
        <Button isOutlineTheme>Refresh</Button>
        <Button isTheme>Create New</Button>
        <ButtonIcon size="medium" isNeutral>
            <IconHelp />
        </ButtonIcon>
    </PageHeader>
  </Story>
</Preview>

