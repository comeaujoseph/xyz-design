import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';

import Breadcrumb from './base';
import { Crumb } from './crumb';

<Meta title="Breadcrumb" component={Breadcrumb} />

# Breadcrumb

...

<Preview withToolbar>
  <Story name="base" parameters={{ decorators: [withKnobs] }}>
    <Breadcrumb>
      <Crumb isOutline>edit</Crumb>
      <Crumb isOutline>save</Crumb>
      <Crumb isLast>delete</Crumb>
    </Breadcrumb>
  </Story>
</Preview>
