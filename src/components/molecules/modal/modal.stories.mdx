import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';

import Modal from './base';

import Tabs from '../tabs/base';
import { Tab } from '../tabs/tab';
import Disclaimer from '../../atoms/disclaimer/base';
import Label from '../../atoms/label/base';

import FormItem from '../form-item/base';

import Input from '../../atoms/input/base';
import Button from '../../atoms/button/base';
import IconSearch from '../../../shared/svg-icons/icon-search';

import Options from '../options/base';
import { SimpleOption, DetailedOption } from '../options/option';

<Meta title="Modal" component={Modal} />

# Modal

...

<Preview withToolbar>
  <Story name="base" parameters={{ decorators: [withKnobs] }}>
    <Modal size="x-small" isOpen>
      <Tabs>
        <Tab isSelected>Configurations</Tab>
        <Tab>Exemptions</Tab>
        <Tab>Alerts</Tab>
      </Tabs>
      <div style={{marginBottom: "20px"}}>
        <Label>1. Select execution scope</Label>
        <Options>
          <DetailedOption title="> 15 days" description="Run module on the last 15 days of artifacts." value={<span>Past <em>15</em> days</span>} isSelected />
          <DetailedOption title="> 30 days" description="Run module on the last 30 days of artifacts." value={<span>Past <em>30</em> days</span>} />
          <DetailedOption title="> 60 days" description="Run module on the last 60 days of artifacts." value={<span>Past <em>60</em> days</span>} />
        </Options>
        <Disclaimer />
      </div>
      <div>
        <Label>2. Select action</Label>
        <Options>
          <SimpleOption title="Add Tag" isSelected />
          <SimpleOption title="Alert Only" />
        </Options>
      </div>
      <FormItem style={{marginTop: "40px"}}>
        <Button isTheme isStretch>Run Module</Button>
      </FormItem>
    </Modal>
  </Story>
</Preview>
