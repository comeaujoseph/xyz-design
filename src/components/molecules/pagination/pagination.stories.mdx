import { ArgsTable, Meta, Preview, Story } from '@storybook/addon-docs/blocks';

import Pagination from './base';

<Meta title="Pagination" component={Pagination} />

# Pagination

<Preview withToolbar>
  <Story name="pagination">
    <Pagination totalPages={20} currentPage={5} isVerbose />
  </Story>
</Preview>
