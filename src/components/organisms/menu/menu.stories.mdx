import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';

import Menu from './base';
import { MenuItem } from './item';
import { SubMenu } from './submenu';

import IconDashboard from '../../../shared/svg-icons/icon-dashboard';
import IconTag from '../../../shared/svg-icons/icon-tag';
import IconPeople from '../../../shared/svg-icons/icon-people';

<Meta title="Menu" component={Menu} />

# Menu

...

<Preview withToolbar>
  <Story name="base" parameters={{ decorators: [withKnobs] }}>
    <Menu title="settings">
      <MenuItem name="item 1" icon={<IconDashboard />} />
      <MenuItem name="item 1" icon={<IconTag />} isActive />
      <MenuItem name="item 1" icon={<IconPeople />} />
    </Menu>
  </Story>
</Preview>


<Preview withToolbar>
  <Story name="submenu" parameters={{ decorators: [withKnobs] }}>
    <Menu title="settings" style={{width: "200px", minHeight: "250px"}}>
      <MenuItem name="item 1" icon={<IconDashboard />} />
      <MenuItem name="item 1" icon={<IconTag />} />
      <SubMenu position="right" size="xx-small" name="item 1" icon={<IconPeople />} isActive>
        <Menu>
          <MenuItem name="sub-option 1" isCenter />
          <MenuItem name="sub-option 2" isCenter />
        </Menu>
      </SubMenu>
    </Menu>
  </Story>
</Preview>

<Preview withToolbar>
  <Story name="inline submenu" parameters={{ decorators: [withKnobs] }}>
    <Menu title="settings" style={{width: "200px", minHeight: "250px"}}>
      <MenuItem name="item 1" icon={<IconDashboard />} />
      <SubMenu mode="inline" name="item 2" icon={<IconPeople />} isActive>
        <Menu>
          <MenuItem name="sub-option 1" />
          <MenuItem name="sub-option 2" />
        </Menu>
      </SubMenu>
      <MenuItem name="item 3" icon={<IconTag />} />
    </Menu>
  </Story>
</Preview>

